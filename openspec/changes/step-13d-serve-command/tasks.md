# Step 13d: Serve command + cutover — tasks

- [ ] 13d.1 Implement `lifekit ui`: start feed API (:8765) + `next start`
  (:3000), localhost only; clean shutdown of both
- [ ] 13d.2 Delete `ui/app.py`; remove Streamlit references from docs and
  `.devcontainer/`
- [ ] 13d.3 Update `docs/product/STATUS.md` (phone-access row) and
  `docs/product/USER_MANUAL.md` with the new run flow
- [ ] 13d.4 End-to-end on the workstation: boot, daily loop in browser,
  phone over tailnet, clean shutdown; suite green
- [ ] 13d.5 Update `docs/product/STATUS.md`; archive all Step 13 slices on completion
