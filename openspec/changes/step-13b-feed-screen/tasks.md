# Step 13b: Feed screen — tasks

- [ ] 13b.1 Scaffold the Next.js app (App Router, TypeScript, Tailwind) in
  `frontend/`; `/api/*` rewrites → `127.0.0.1:8765`
- [ ] 13b.2 Build the seven feed components per the umbrella's visual spec,
  pixel-faithful to the approved mock
- [ ] 13b.3 Wire Mark done → `POST /api/completions`; Still with me →
  `POST /api/idea-signals`; verify persistence in the DB
- [ ] 13b.4 Wire the conversations list (reopen) and the master composer
  (create → route to `/c/[id]` stub)
- [ ] 13b.5 Visual pass against the mock: layout, spacing, theme; no quiz
  framing or extra tabs
- [ ] 13b.6 Update `docs/product/STATUS.md`; archive this slice on completion
